# Story 1.3 mutation classifications

The complete first pass tested 193 mutations. Test strengthening and small lifecycle/validation extractions produced a final inventory of 195. Runs 3 and 4 reran survivors and every changed production function. The machine-readable final inventory is in [1-3-mutation-results.json](1-3-mutation-results.json).

## Final inventory

- 195 mutations accounted for: 171 CaughtMutant, 4 MissedMutant, 20 Unviable.
- Unviable mutations did not compile and are not counted as caught. No timeout is treated as a success.

## Remaining survivors

- `src/adapters/loopback_ui.rs:81:43: replace match guard peer.ip().is_loopback() with true in LoopbackUi::serve` — Defence-in-depth under supported deployment: the only listener is bound to literal IPv4 127.0.0.1. Tests exercise that listener and the independent launch/cookie/Origin/CSRF boundary. A non-loopback accepted peer would require host routing outside these fixtures; the production peer check is retained.
- `src/adapters/loopback_ui.rs:90:27: replace match guard e.kind() == std::io::ErrorKind::WouldBlock with true in LoopbackUi::serve` — Explicit OS-fault injection gap: tests exercise WouldBlock and successful accepts, but do not force another accept errno. This mutant could retry a broken listener instead of returning BackendUnavailable. Production retains fail-closed error handling; no authentication or backend resolution occurs on this branch. This is not claimed equivalent or caught.
- `src/adapters/session.rs:78:40: replace | with ^ in load_setup` — Equivalent for the Linux constants: O_NOFOLLOW and O_NONBLOCK have disjoint bits, so bitwise OR and XOR produce the same open flags.
- `src/adapters/session.rs:85:9: replace || with && in load_setup` — Explicit OS-fixture gap: tests use current-UID regular files and reject modes, hard links and symlinks, but do not supply a readable foreign-UID file or a synchronized nonregular input stream. The mutant weakens the file-type/owner predicate. Production retains both independent checks. This is not claimed equivalent or caught.

## Individual disposition of all 37 original survivors

- `src/bin/vaultwarden-accessd.rs:66:19: replace match guard e.kind() == std::io::ErrorKind::NotFound with true in run` — Fixed coverage: extracted the existing launch cleanup operation and tested absent, stale-file and failed-removal cases. Corresponding helper mutations are caught in run 3.
- `src/bin/vaultwarden-accessd.rs:66:19: replace match guard e.kind() == std::io::ErrorKind::NotFound with false in run` — Fixed coverage: extracted the existing launch cleanup operation and tested absent, stale-file and failed-removal cases. Corresponding helper mutations are caught in run 3.
- `src/bin/vaultwarden-accessd.rs:66:28: replace == with != in run` — Fixed coverage: extracted the existing launch cleanup operation and tested absent, stale-file and failed-removal cases. Corresponding helper mutations are caught in run 3.
- `src/access/application.rs:5:58: replace * with +` — Fixed coverage: independent 899/900-second cap expectations and zero-duration rejection. Mutation caught in run 3; deadline overflow also now revokes newly created authority.
- `src/access/application.rs:133:29: replace match guard !lifetime.is_zero() with true in <impl ApprovalAuthenticator for ProviderApplication>::authenticate` — Fixed coverage: independent 899/900-second cap expectations and zero-duration rejection. Mutation caught in run 3; deadline overflow also now revokes newly created authority.
- `src/access/ports.rs:62:9: replace <impl fmt::Display for SessionError>::fmt -> fmt::Result with Ok(Default::default())` — Fixed coverage: exact stable redacted diagnostics are asserted for every SessionError. Caught in run 3.
- `src/adapters/loopback_ui.rs:81:43: replace match guard peer.ip().is_loopback() with true in LoopbackUi::serve` — Defence-in-depth under supported deployment: the only listener is bound to literal IPv4 127.0.0.1. Tests exercise that listener and the independent launch/cookie/Origin/CSRF boundary. A non-loopback accepted peer would require host routing outside these fixtures; the production peer check is retained.
- `src/adapters/loopback_ui.rs:90:27: replace match guard e.kind() == std::io::ErrorKind::WouldBlock with true in LoopbackUi::serve` — Explicit OS-fault injection gap: tests exercise WouldBlock and successful accepts, but do not force another accept errno. This mutant could retry a broken listener instead of returning BackendUnavailable. Production retains fail-closed error handling; no authentication or backend resolution occurs on this branch. This is not claimed equivalent or caught.
- `src/adapters/loopback_ui.rs:160:84: replace > with >= in LoopbackUi::handle` — Fixed coverage: exactly 4096 password bytes reach authentication, while 4097 are rejected before authentication. Caught in run 3.
- `src/adapters/session.rs:28:9: replace <impl SessionClock for MonotonicClock>::now -> Duration with Default::default()` — Fixed coverage: the production monotonic clock must advance across a bounded sleep. Caught in run 3.
- `src/adapters/session.rs:78:40: replace | with & in load_setup` — Fixed coverage: otherwise-valid setup JSON tests unsafe modes, hard links, symlinks, forbidden authority fields and exact 65536-byte boundary. Caught in run 3.
- `src/adapters/session.rs:87:9: replace || with && in load_setup` — Fixed coverage: otherwise-valid setup JSON tests unsafe modes, hard links, symlinks, forbidden authority fields and exact 65536-byte boundary. Caught in run 3.
- `src/adapters/session.rs:78:40: replace | with ^ in load_setup` — Equivalent for the Linux constants: O_NOFOLLOW and O_NONBLOCK have disjoint bits, so bitwise OR and XOR produce the same open flags.
- `src/adapters/session.rs:86:9: replace || with && in load_setup` — Fixed coverage: otherwise-valid setup JSON tests unsafe modes, hard links, symlinks, forbidden authority fields and exact 65536-byte boundary. Caught in run 3.
- `src/adapters/session.rs:85:9: replace || with && in load_setup` — Explicit OS-fixture gap: tests use current-UID regular files and reject modes, hard links and symlinks, but do not supply a readable foreign-UID file or a synchronized nonregular input stream. The mutant weakens the file-type/owner predicate. Production retains both independent checks. This is not claimed equivalent or caught.
- `src/adapters/session.rs:96:20: replace > with == in load_setup` — Fixed coverage: otherwise-valid setup JSON tests unsafe modes, hard links, symlinks, forbidden authority fields and exact 65536-byte boundary. Caught in run 3.
- `src/adapters/session.rs:96:20: replace > with >= in load_setup` — Fixed coverage: otherwise-valid setup JSON tests unsafe modes, hard links, symlinks, forbidden authority fields and exact 65536-byte boundary. Caught in run 3.
- `src/adapters/session.rs:135:25: replace > with >= in derive_keys` — Fixed coverage: extracted inexpensive KDF admission validation and independently asserted 1,999,999 / 2,000,000 / 2,000,001 iteration boundaries before derivation. Corresponding mutations caught in run 3.
- `src/adapters/session.rs:142:37: replace || with && in derive_keys` — Fixed coverage: isolated test process enables the CLI insecure-MAC override and still requires provider rejection of valid ciphertext with its MAC removed. Caught in run 4.
- `src/adapters/session.rs:135:25: replace > with == in derive_keys` — Fixed coverage: extracted inexpensive KDF admission validation and independently asserted 1,999,999 / 2,000,000 / 2,000,001 iteration boundaries before derivation. Corresponding mutations caught in run 3.
- `src/adapters/vaultwarden.rs:79:24: replace > with == in VaultwardenBackend::body` — Fixed coverage: valid JSON padded to exactly 1 MiB is accepted; one extra byte is rejected. Caught in run 3.
- `src/adapters/vaultwarden.rs:79:24: replace > with >= in VaultwardenBackend::body` — Fixed coverage: valid JSON padded to exactly 1 MiB is accepted; one extra byte is rejected. Caught in run 3.
- `src/adapters/vaultwarden.rs:110:13: replace || with && in VaultwardenBackend::selected` — Fixed coverage: wrong/deleted/type-mismatched items, malformed IDs, empty bindings and ambiguous markers/fields are rejected using otherwise valid fixtures. Caught in run 3.
- `src/adapters/vaultwarden.rs:103:13: replace || with && in VaultwardenBackend::selected` — Fixed coverage: wrong/deleted/type-mismatched items, malformed IDs, empty bindings and ambiguous markers/fields are rejected using otherwise valid fixtures. Caught in run 3.
- `src/adapters/vaultwarden.rs:125:13: replace || with && in VaultwardenBackend::selected` — Fixed coverage: wrong/deleted/type-mismatched items, malformed IDs, empty bindings and ambiguous markers/fields are rejected using otherwise valid fixtures. Caught in run 3.
- `src/adapters/vaultwarden.rs:124:13: replace || with && in VaultwardenBackend::selected` — Fixed coverage: wrong/deleted/type-mismatched items, malformed IDs, empty bindings and ambiguous markers/fields are rejected using otherwise valid fixtures. Caught in run 3.
- `src/adapters/vaultwarden.rs:135:43: replace || with && in VaultwardenBackend::selected` — Fixed coverage: isolated insecure-MAC override tests exercise authenticated private-key and login-field requirements using otherwise valid ciphertext. Caught in run 4.
- `src/adapters/vaultwarden.rs:155:41: replace || with && in VaultwardenBackend::selected` — Fixed coverage: isolated insecure-MAC override tests exercise authenticated private-key and login-field requirements using otherwise valid ciphertext. Caught in run 4.
- `src/adapters/vaultwarden.rs:274:13: replace || with && in <impl ProviderSession for VaultwardenBackend>::unlock` — Fixed coverage: expired/empty/non-Bearer tokens and mismatched KDF/key metadata fail before persistence; matching optional metadata succeeds. Caught in run 3.
- `src/adapters/vaultwarden.rs:272:13: replace || with && in <impl ProviderSession for VaultwardenBackend>::unlock` — Fixed coverage: expired/empty/non-Bearer tokens and mismatched KDF/key metadata fail before persistence; matching optional metadata succeeds. Caught in run 3.
- `src/adapters/vaultwarden.rs:273:13: replace || with && in <impl ProviderSession for VaultwardenBackend>::unlock` — Fixed coverage: expired/empty/non-Bearer tokens and mismatched KDF/key metadata fail before persistence; matching optional metadata succeeds. Caught in run 3.
- `src/adapters/vaultwarden.rs:268:13: replace || with && in <impl ProviderSession for VaultwardenBackend>::unlock` — Fixed coverage: expired/empty/non-Bearer tokens and mismatched KDF/key metadata fail before persistence; matching optional metadata succeeds. Caught in run 3.
- `src/adapters/vaultwarden.rs:265:13: replace || with && in <impl ProviderSession for VaultwardenBackend>::unlock` — Fixed coverage: expired/empty/non-Bearer tokens and mismatched KDF/key metadata fail before persistence; matching optional metadata succeeds. Caught in run 3.
- `src/adapters/vaultwarden.rs:266:34: replace != with == in <impl ProviderSession for VaultwardenBackend>::unlock` — Fixed coverage: expired/empty/non-Bearer tokens and mismatched KDF/key metadata fail before persistence; matching optional metadata succeeds. Caught in run 3.
- `src/adapters/vaultwarden.rs:264:36: replace != with == in <impl ProviderSession for VaultwardenBackend>::unlock` — Fixed coverage: expired/empty/non-Bearer tokens and mismatched KDF/key metadata fail before persistence; matching optional metadata succeeds. Caught in run 3.
- `src/adapters/vaultwarden.rs:271:46: replace != with == in <impl ProviderSession for VaultwardenBackend>::unlock` — Fixed coverage: expired/empty/non-Bearer tokens and mismatched KDF/key metadata fail before persistence; matching optional metadata succeeds. Caught in run 3.
- `src/adapters/vaultwarden.rs:290:9: replace <impl SecretBackend for VaultwardenBackend>::eligible -> Result<bool, SessionError> with Ok(true)` — Fixed coverage: exact stable redacted diagnostics are asserted for every SessionError. Caught in run 3.
