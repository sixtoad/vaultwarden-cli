# Story 1.4 independent review

All three context-free BMAD reviewers ran at the parent model capability and completed before triage. Reviewed snapshot: `/tmp/vw-story14-review-index-jb4ortnm/complete.diff`. Initial agent-capacity failure was resolved by a retry; external review packets are no longer required.

Review retry succeeded: all three context-free reviewers completed before triage. Each finding is classified below before grouping. The blind review arithmetic line is not a defect finding.

| ID | Finding | Verdict | Evidence and route |
| --- | --- | --- | --- |
| B1 | Queued submissions outlive client timeout | medium | `submit_direct` holds the blocking authority mutex through desktop handoff; 16 workers can queue while clients allow 10 seconds. Later creation can outlive receipt delivery. Patch: reject contention before admission/creation, using the existing unavailable category; add synchronized concurrency coverage. |
| B2 | Post-persistence revocation loses receipt | medium | `create_direct` may succeed before the subsequent `admit()?` revokes and returns Locked, hiding the persisted ID. Patch: preserve a successful creation result and return its terminal receipt after revocation, without launching revoked work. |
| B3 | Launcher descendants can survive timeout | low | `run_bounded` kills and reaps the immediate fixed `/usr/bin/xdg-open` child; it does not supervise descendants. The timeout fixture uses `exec` and proves only immediate-child cleanup. A detached browser is an intended desktop handoff. On opener failure the broker never registers the capability and drops/removes its artifact, so surviving descendants cannot exchange that failed launch capability. Descendant resource cleanup remains unverified; process-tree supervision is outside this patch. |
| B4 | Browser capacity never reclaims lost cookies | low | The provider retains up to 64 browser sessions until restart and has no reclamation path. Each valid exchange lacking a recognized cookie consumes a new slot; repeated cookie loss in the same browser can exhaust the bound, without 64 distinct browsers. Existing recognized sessions remain usable; at capacity a fresh exchange consumes its one-use capability and returns 403. Restart is the documented recovery. No frequency or latency evidence establishes that exhaustion is rare. |
| B5 | Concurrent first browser launches race cookies | medium | Two initial requests can carry no cookie even though their server handlers serialize; their Set-Cookie responses select different sessions while per-tab proofs persist. Patch: serialize first launch exchange across same-origin tabs before fetch, retaining one-use and independent-proof semantics; verify with real concurrent fresh-browser tabs. |
| B6 | Transient review failure stops polling | medium | The catch branch sets text but never schedules another read after clearing the timer. Patch: bounded retry delay/backoff so recovery/expiry becomes observable automatically; exercise an actual transient fetch failure. |
| B7 | Argument boundaries are ambiguous | false | Target and Choice values must satisfy `valid_value`, which requires nonempty ASCII graphic bytes; normalized Integer values are decimal. Consequently admitted arguments cannot contain spaces or the U+00B7 separator in ` · `, and distinct argument vectors do not collide under that join. Quotes are permitted. Numbered argument rows improve positional labeling/readability, but separator-containing DTO/DOM fixtures are defensive renderer checks, not evidence of valid policy admission. Do not relax policy validation to construct such a fixture. |
| B8 | Unchanged polls rebuild details/live status | low | Every successful poll replaces detail nodes and assigns the same atomic status text, disrupting selection and producing unnecessary live-region mutations. Everyday behavior with a direct correction: patch immutable-detail reuse and status updates only on change; verify DOM stability. |
| B9 | Prune repeatedly scans complete history | low | Once due, `serve` synchronously prunes up to 64 launch IDs before accepting new HTTPS connections. Each `direct_status` calls `review_direct`, which expires requests and reads the review: normally two full validated state reads per ID, including executable identity hashing, plus any writes on expiry. History has no pruning bound. This can delay new loopback connections and contend with human operations; no large-history or large-executable benchmark establishes the impact. Retaining this algorithm accepts an unmeasured availability limitation, rather than proving it is low-impact or uncommon. |
| B10 | Subcommand help shows root help | low | DisplayHelp unconditionally rebuilds the root command, omitting request-specific revision/no-wait/separator help. Everyday defect with a direct correction: patch static selected-subcommand help without rejected-input reflection. |
| E1 | Queued submissions lose receipts | medium | Same verified authority-mutex/client-timeout defect as B1; patch in the B1 group. |
| E2 | Concurrent browser bootstrap overwrites cookie | medium | Same verified fresh-profile cookie/proof race as B5; patch in the B5 group. |
| V1 | Configured lifetime wiring lacks verification | medium | Pre-verified gap: parser and constructor tests do not detect replacing the daemon's configured duration with 300. Patch a test through the production configuration-to-application construction path, asserting receipt and exact monotonic expiry. |
| V2 | Human worker limit lacks saturation verification | medium | Pre-verified gap: kernel-backlog and reap-helper tests do not remove/observe the running server admission-limit branch. Patch a synchronized real-socket 16-worker rejection/recovery test. |

Survivor groups: B1/E1, B2, B5/E2, B6, B8, B10, V1, V2. All route to patch: each has a direct private correction or test for a demonstrated state, with no new public authority/API. No intent_gap, bad_spec or defer entries remain. All eight groups were patched and final verification completed; see the evidence report.

Post-patch audit corrected B7 to false after tracing policy admission. Numbered argument rows remain a readability/accessibility improvement. B3/B4/B9 remain accepted limitations with their exact cleanup, capacity, and unmeasured performance bounds disclosed above; no rarity or measured latency claim is made. Eight defect/test-gap groups were patched. Root full verification completed on frozen source.

Post-review verification complete on frozen source: all-targets 685 reported passes (614 exercised bodies plus 71 environment-gated live entries), zero failures; three fixture entries explicitly exercised; 13 benchmark smokes. Format, all-target/all-feature Clippy, diff checks, and real Firefox passed (24 axe checks, zero violations). Current generated scope 347 = 299 caught + 42 compiler-rejected + 3 observable equivalents + 1 bounded-time contract equivalent + 2 inactive non-Linux; 24 supplementary manual mutations caught. Run 4 completed all 44 selected mutants, exit 0; 303 results have exact complete-context carry evidence. All required runs have finished; no unresolved survivor/timeouts/unrelated-failure catches. Browser-only harness follow-up retries exact transient admission rejection; full verification and all six browser mutations reran afterward. Evidence: `docs/implementation/1-4-test-evidence.md` and `docs/implementation/1-4-evidence/`. No commit, push or PR.

Human approval of the completed implementation and evidence was received on 2026-09-24. The local commit may proceed; no push or PR was requested. Operation-policy provisioning currently has a provider API but no user-facing setup command; this limits everyday usability alongside the intentionally absent decision/execution stages.
